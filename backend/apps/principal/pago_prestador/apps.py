from django.apps import AppConfig

class PagoPrestadorConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.principal.pago_prestador'
    verbose_name = 'Pago a Prestadores'
